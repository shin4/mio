# Mio Desktop

Electron shell for the Mio app.

## Development

From the repository root:

```bash
bun run dev:desktop
```

From this package:

```bash
bun dev
```

## Build

```bash
bun build
bun package
```

The packaged application is written to `dist/`.
