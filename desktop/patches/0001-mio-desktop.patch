diff --git a/apps/desktop-host/package.json b/apps/desktop-host/package.json
index a5b679b..f4be958 100644
--- a/apps/desktop-host/package.json
+++ b/apps/desktop-host/package.json
@@ -23,6 +23,7 @@
     "@deepseek-ai/dsh-tool-workspace-dependencies": "workspace:*",
     "@deepseek-ai/dsh-skill-office": "workspace:*",
     "@deepseek-ai/dsh-deepseek-account": "workspace:*",
-    "@deepseek-ai/dsh-workspace": "workspace:*"
+    "@deepseek-ai/dsh-workspace": "workspace:*",
+    "@mio/desktop": "workspace:*"
   }
 }
diff --git a/apps/desktop-host/src/index.ts b/apps/desktop-host/src/index.ts
index 4a47a92..dd8f57c 100644
--- a/apps/desktop-host/src/index.ts
+++ b/apps/desktop-host/src/index.ts
@@ -27,7 +27,7 @@ async function main(): Promise<void> {
     profile: 'desktop',
     resolvedProfile: { profile, installAnchor },
     patchFiles: [],
-    args: ['--no-open', '--port', '19387'],
+    args: ['--no-open', '--port', '0'],
     ...(process.argv[5] === undefined ? {} : {
       packageManager: {
         command: process.execPath,
diff --git a/apps/desktop/installer/extract-report.h b/apps/desktop/installer/extract-report.h
index c885816..dfe8b45 100644
--- a/apps/desktop/installer/extract-report.h
+++ b/apps/desktop/installer/extract-report.h
@@ -114,7 +114,7 @@ inline std::wstring Headline(int code, const std::wstring& output, const std::ws
 
 inline std::wstring Compose(int code, const std::wstring& archive, const std::wstring& destination, const std::wstring& output,
                             const std::wstring& timestamp, const std::wstring& windowsVersion) {
-    std::wstring report = L"DeepSeek Harness installer: extraction failed\r\n";
+    std::wstring report = L"Mio installer: extraction failed\r\n";
     report += L"Time: " + timestamp + L"\r\n";
     report += L"Result: " + DescribeResult(code) + L"\r\n";
     report += L"Archive: " + archive + L"\r\n";
diff --git a/apps/desktop/installer/strings.nsh b/apps/desktop/installer/strings.nsh
index 174ebfa..42ead8c 100644
--- a/apps/desktop/installer/strings.nsh
+++ b/apps/desktop/installer/strings.nsh
@@ -19,20 +19,20 @@ LangString INSTALLER_BROWSE ${LANG_ENGLISH} "Browse"
 LangString INSTALLER_BROWSE ${LANG_SIMPCHINESE} "选择"
 LangString INSTALLER_LAUNCH ${LANG_ENGLISH} "Launch now"
 LangString INSTALLER_LAUNCH ${LANG_SIMPCHINESE} "立即启动"
-LangString INSTALLER_LAUNCH_FAILED ${LANG_ENGLISH} "DeepSeek Harness could not be launched. Retry, or clear Launch now and close the installer."
-LangString INSTALLER_LAUNCH_FAILED ${LANG_SIMPCHINESE} "无法启动 DeepSeek Harness。请重试，或取消勾选立即启动后关闭安装程序。"
+LangString INSTALLER_LAUNCH_FAILED ${LANG_ENGLISH} "Mio could not be launched. Retry, or clear Launch now and close the installer."
+LangString INSTALLER_LAUNCH_FAILED ${LANG_SIMPCHINESE} "无法启动 Mio。请重试，或取消勾选立即启动后关闭安装程序。"
 LangString INSTALLER_UI_ERROR ${LANG_ENGLISH} "The installer could not initialize its window. Close it and try again."
 LangString INSTALLER_UI_ERROR ${LANG_SIMPCHINESE} "无法初始化安装窗口，请关闭后重试。"
 LangString INSTALLER_THEME_ERROR ${LANG_ENGLISH} "Use /THEME=auto, /THEME=light or /THEME=dark."
 LangString INSTALLER_THEME_ERROR ${LANG_SIMPCHINESE} "主题参数须为 /THEME=auto、/THEME=light 或 /THEME=dark。"
 LangString INSTALLER_FAILED ${LANG_ENGLISH} "Installation did not finish. Close this installer and retry. If the problem persists, download the installer again."
 LangString INSTALLER_FAILED ${LANG_SIMPCHINESE} "安装未完成，请关闭安装程序后重试。如仍然失败，请重新下载安装包。"
-LangString INSTALLER_RUNNING ${LANG_ENGLISH} "DeepSeek Harness is running. Quit it from the system tray and try again."
-LangString INSTALLER_RUNNING ${LANG_SIMPCHINESE} "DeepSeek Harness 正在运行，请先在系统托盘中退出应用后重试。"
+LangString INSTALLER_RUNNING ${LANG_ENGLISH} "Mio is running. Quit it from the system tray and try again."
+LangString INSTALLER_RUNNING ${LANG_SIMPCHINESE} "Mio 正在运行，请先在系统托盘中退出应用后重试。"
 LangString INSTALLER_PATH_INVALID ${LANG_ENGLISH} "Choose a full local folder path. Drive roots, system folders, links and special characters are not supported."
 LangString INSTALLER_PATH_INVALID ${LANG_SIMPCHINESE} "请选择本地磁盘上的完整文件夹路径，不能使用磁盘根目录、系统目录、链接目录或包含特殊字符的路径。"
-LangString INSTALLER_PATH_OWNERSHIP ${LANG_ENGLISH} "Choose an empty folder or the registered DeepSeek Harness installation folder."
-LangString INSTALLER_PATH_OWNERSHIP ${LANG_SIMPCHINESE} "请选择空文件夹，或 DeepSeek Harness 原来的安装目录。"
+LangString INSTALLER_PATH_OWNERSHIP ${LANG_ENGLISH} "Choose an empty folder or the registered Mio installation folder."
+LangString INSTALLER_PATH_OWNERSHIP ${LANG_SIMPCHINESE} "请选择空文件夹，或 Mio 原来的安装目录。"
 LangString INSTALLER_PATH_WRITABLE ${LANG_ENGLISH} "This location is not writable. Choose a folder available to the current user."
 LangString INSTALLER_PATH_WRITABLE ${LANG_SIMPCHINESE} "无法写入此位置。请选择当前用户可写入的文件夹；安装程序不会申请管理员权限。"
 LangString INSTALLER_DISK_SPACE ${LANG_ENGLISH} "There is not enough free disk space. Choose another location."
diff --git a/apps/desktop/installer/uninstall.nsh b/apps/desktop/installer/uninstall.nsh
index defdd15..92e56a8 100644
--- a/apps/desktop/installer/uninstall.nsh
+++ b/apps/desktop/installer/uninstall.nsh
@@ -25,12 +25,5 @@ Function un.CleanData
   ClearErrors
   StrCpy $UnTarget "$APPDATA\${PRODUCT_FILENAME}"
   Call un.RemoveData
-  !ifdef APP_PACKAGE_NAME
-    ; Electron derives user data from the package name; a scoped name nests it one directory deeper.
-    StrCpy $UnTarget "$APPDATA\${APP_PACKAGE_NAME}"
-    Call un.RemoveData
-    System::Call '$PLUGINSDIR\window-frame.dll::UninstallRemoveEmptyParents(w "$UnTarget", w "$APPDATA") ?c'
-  !endif
-  StrCpy $UnTarget "$LOCALAPPDATA\${DSH_UPDATER_CACHE_NAME}"
-  Call un.RemoveData
+  ; Mio sets app.name explicitly and does not own the upstream package-name or updater cache directories.
 FunctionEnd
diff --git a/apps/desktop/renderer/assets/welcome-brand.svg b/apps/desktop/renderer/assets/welcome-brand.svg
index 67c917d..a708103 100644
--- a/apps/desktop/renderer/assets/welcome-brand.svg
+++ b/apps/desktop/renderer/assets/welcome-brand.svg
@@ -1,44 +1 @@
-<svg width="472" height="40" viewBox="0 0 472 40" fill="none" xmlns="http://www.w3.org/2000/svg">
-<style>
-  :root {
-    --dsw-alias-label-primary: #0f1115;
-    --dsw-alias-label-primary-inverted: #fff;
-  }
-  @media (prefers-color-scheme: dark) {
-    :root {
-      --dsw-alias-label-primary: #f9fafb;
-      --dsw-alias-label-primary-inverted: #353638;
-    }
-  }
-</style>
-<path d="M213.743 26.2447H212.377V24.1272H213.743C214.589 24.1272 215.443 23.9163 215.994 23.3304C216.545 22.7444 216.753 21.8455 216.753 20.9471C216.753 20.0487 216.554 19.1498 215.994 18.5643C215.435 17.9783 214.589 17.7674 213.743 17.7674C212.897 17.7674 212.042 17.9783 211.491 18.5643C210.94 19.1503 210.732 20.0487 210.732 20.9471V29.6435H208.336V15.6582H210.732V16.5488H211.171C211.219 16.4939 211.266 16.4473 211.314 16.4006C211.914 15.8535 212.832 15.6582 213.734 15.6582C215.148 15.6582 216.561 16.01 217.488 16.9866C218.414 17.9632 218.741 19.4632 218.741 20.9558C218.741 22.4485 218.405 23.9406 217.488 24.925C216.569 25.9094 215.148 26.2457 213.743 26.2457V26.2447Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M177.282 16.0355H178.647V18.153H177.282C176.436 18.153 175.581 18.3638 175.03 18.9498C174.479 19.5358 174.272 20.4347 174.272 21.3331C174.272 22.2315 174.472 23.1299 175.03 23.7159C175.589 24.3018 176.436 24.5127 177.282 24.5127C178.128 24.5127 178.983 24.3018 179.534 23.7159C180.085 23.1299 180.293 22.2315 180.293 21.3331V12.6289H182.688V26.6224H180.293V25.7318H179.853C179.805 25.7862 179.758 25.8334 179.709 25.88C179.11 26.4266 178.192 26.6224 177.29 26.6224C175.876 26.6224 174.463 26.2711 173.537 25.2945C172.611 24.3179 172.283 22.8175 172.283 21.3253C172.283 19.8331 172.618 18.3405 173.537 17.3561C174.463 16.3795 175.876 16.0355 177.282 16.0355Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M194.706 21.1444V21.9962H188.325V20.3009H192.557C192.462 19.6838 192.238 19.1056 191.839 18.6834C191.264 18.0741 190.377 17.8554 189.499 17.8554C188.621 17.8554 187.735 18.0741 187.16 18.6834C186.585 19.2927 186.377 20.2227 186.377 21.1449C186.377 22.0671 186.584 23.0044 187.16 23.6059C187.735 24.2074 188.621 24.4343 189.499 24.4343C190.378 24.4343 191.264 24.2157 191.839 23.6059C191.918 23.5199 191.99 23.4261 192.062 23.3323H194.426C194.219 24.067 193.891 24.7312 193.396 25.2467C192.438 26.2545 190.961 26.6218 189.499 26.6218C188.038 26.6218 186.561 26.2622 185.602 25.2467C184.644 24.2312 184.293 22.6842 184.293 21.1449C184.293 19.6056 184.636 18.0508 185.602 17.043C186.569 16.0353 188.038 15.668 189.499 15.668C190.961 15.668 192.438 16.0275 193.396 17.043C194.362 18.0585 194.706 19.6056 194.706 21.1449V21.1444Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M206.731 21.1444V21.9962H200.351V20.3009H204.583C204.487 19.6838 204.264 19.1056 203.864 18.6834C203.289 18.0741 202.403 17.8554 201.525 17.8554C200.647 17.8554 199.76 18.0741 199.185 18.6834C198.61 19.2927 198.402 20.2227 198.402 21.1449C198.402 22.0671 198.61 23.0044 199.185 23.6059C199.76 24.2074 200.647 24.4343 201.525 24.4343C202.403 24.4343 203.289 24.2157 203.864 23.6059C203.944 23.5199 204.016 23.4261 204.087 23.3323H206.452C206.244 24.067 205.916 24.7312 205.421 25.2467C204.463 26.2545 202.986 26.6218 201.525 26.6218C200.064 26.6218 198.586 26.2622 197.628 25.2467C196.67 24.2312 196.318 22.6842 196.318 21.1449C196.318 19.6056 196.662 18.0508 197.628 17.043C198.594 16.0353 200.063 15.668 201.525 15.668C202.986 15.668 204.463 16.0275 205.421 17.043C206.387 18.0585 206.731 19.6056 206.731 21.1449V21.1444Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M225.57 26.6228C227.031 26.6228 228.508 26.4119 229.466 25.8104C230.424 25.2088 230.776 24.2944 230.776 23.3882C230.776 22.482 230.432 21.5598 229.466 20.9661C228.508 20.3723 227.031 20.1532 225.57 20.1532C224.946 20.1532 224.371 20.0672 223.964 19.8408C223.557 19.6061 223.405 19.2548 223.405 18.903C223.405 18.5512 223.549 18.1921 223.964 17.9652C224.371 17.7306 225.003 17.6528 225.625 17.6528C226.247 17.6528 226.879 17.7388 227.287 17.9652C227.694 18.1999 227.845 18.5512 227.845 18.903H230.281C230.281 17.9963 229.97 17.0746 229.1 16.4809C228.229 15.8871 226.896 15.668 225.57 15.668C224.245 15.668 222.911 15.8788 222.041 16.4809C221.17 17.0824 220.858 17.9963 220.858 18.903C220.858 19.8097 221.17 20.7314 222.041 21.3251C222.911 21.9189 224.244 22.138 225.57 22.138C226.257 22.138 227.016 22.224 227.47 22.4505C227.926 22.6774 228.086 23.0364 228.086 23.3882C228.086 23.74 227.926 24.0991 227.47 24.326C227.016 24.5524 226.321 24.6384 225.634 24.6384C224.948 24.6384 224.245 24.5524 223.797 24.326C223.35 24.0996 223.182 23.74 223.182 23.3882H220.371C220.371 24.2949 220.714 25.2166 221.681 25.8104C222.647 26.4041 224.109 26.6228 225.57 26.6228H225.57Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M242.801 21.1444V21.9962H236.421V20.3009H240.653C240.557 19.6838 240.333 19.1056 239.934 18.6834C239.359 18.0741 238.473 17.8554 237.594 17.8554C236.715 17.8554 235.829 18.0741 235.254 18.6834C234.68 19.2927 234.473 20.2227 234.473 21.1449C234.473 22.0671 234.68 23.0044 235.254 23.6059C235.829 24.2074 236.716 24.4343 237.594 24.4343C238.472 24.4343 239.359 24.2157 239.934 23.6059C240.014 23.5199 240.086 23.4261 240.158 23.3323H242.521C242.313 24.067 241.986 24.7312 241.491 25.2467C240.533 26.2545 239.056 26.6218 237.594 26.6218C236.132 26.6218 234.656 26.2622 233.698 25.2467C232.739 24.2312 232.389 22.6842 232.389 21.1449C232.389 19.6056 232.732 18.0508 233.698 17.043C234.664 16.0353 236.133 15.668 237.594 15.668C239.055 15.668 240.533 16.0275 241.491 17.043C242.457 18.0585 242.8 19.6056 242.8 21.1449L242.801 21.1444Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M254.827 21.1444V21.9962H248.446V20.3009H252.679C252.583 19.6838 252.359 19.1056 251.96 18.6834C251.385 18.0741 250.499 17.8554 249.62 17.8554C248.741 17.8554 247.856 18.0741 247.281 18.6834C246.706 19.2927 246.498 20.2227 246.498 21.1449C246.498 22.0671 246.706 23.0044 247.281 23.6059C247.856 24.2074 248.742 24.4343 249.62 24.4343C250.498 24.4343 251.385 24.2157 251.96 23.6059C252.04 23.5199 252.111 23.4261 252.184 23.3323H254.547C254.339 24.067 254.012 24.7312 253.518 25.2467C252.559 26.2545 251.082 26.6218 249.62 26.6218C248.159 26.6218 246.682 26.2622 245.724 25.2467C244.766 24.2312 244.414 22.6842 244.414 21.1449C244.414 19.6056 244.757 18.0508 245.724 17.043C246.69 16.0353 248.159 15.668 249.62 15.668C251.081 15.668 252.558 16.0275 253.518 17.043C254.484 18.0585 254.827 19.6056 254.827 21.1449V21.1444Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M258.827 12.6289H256.432V26.6224H258.827V12.6289Z" fill="var(--dsw-alias-label-primary)"/>
-<path d="M262.917 20.8169L266.844 26.6222H263.882L259.953 20.8169L263.882 16.1523H266.844L262.917 20.8169Z" fill="var(--dsw-alias-label-primary)"/>
-<g clip-path="url(#clip0_2141_5628)">
-<path d="M168.384 12.9517C168.138 12.8317 168.033 13.0607 167.889 13.1767C167.84 13.2142 167.799 13.2632 167.757 13.3082C167.399 13.6912 166.98 13.9427 166.433 13.9127C165.632 13.8677 164.949 14.1192 164.345 14.7312C164.217 13.9767 163.79 13.5262 163.141 13.2372C162.802 13.0872 162.458 12.9367 162.22 12.6102C162.054 12.3776 162.009 12.1186 161.926 11.8631C161.873 11.7091 161.82 11.5516 161.643 11.5251C161.45 11.4951 161.375 11.6566 161.299 11.7916C160.997 12.3436 160.88 12.9517 160.892 13.5677C160.918 14.9532 161.503 16.0568 162.666 16.8418C162.798 16.9318 162.832 17.0218 162.79 17.1533C162.711 17.4238 162.617 17.6863 162.534 17.9568C162.481 18.1293 162.402 18.1673 162.217 18.0918C161.579 17.8253 161.028 17.4308 160.541 16.9543C159.714 16.1548 158.967 15.2722 158.035 14.5812C157.816 14.4197 157.597 14.2697 157.37 14.1267C156.419 13.2032 157.495 12.4446 157.744 12.3546C158.004 12.2606 157.834 11.9376 156.993 11.9416C156.151 11.9451 155.381 12.2271 154.4 12.6027C154.256 12.6592 154.105 12.7002 153.951 12.7342C153.06 12.5652 152.135 12.5277 151.169 12.6367C149.35 12.8392 147.897 13.6992 146.829 15.1672C145.546 16.9318 145.244 18.9368 145.613 21.0279C146.002 23.232 147.127 25.0565 148.855 26.4831C150.648 27.9621 152.713 28.6871 155.068 28.5481C156.499 28.4656 158.091 28.2741 159.888 26.7536C160.341 26.9791 160.816 27.0691 161.605 27.1366C162.212 27.1931 162.797 27.1066 163.25 27.0126C163.96 26.8626 163.911 26.2056 163.654 26.0851C161.575 25.1165 162.031 25.5105 161.616 25.1915C162.673 23.9415 164.287 21.7194 164.785 18.6938C164.834 18.3598 164.896 17.8893 164.889 17.6188C164.885 17.4538 164.923 17.3898 165.111 17.3708C165.632 17.3108 166.138 17.1683 166.603 16.9128C167.95 16.1768 168.494 14.9677 168.622 13.5187C168.641 13.2972 168.618 13.0682 168.384 12.9517ZM156.642 25.9951C154.626 24.4105 153.649 23.889 153.245 23.9115C152.868 23.934 152.936 24.366 153.019 24.6475C153.106 24.9255 153.219 25.117 153.377 25.361C153.487 25.5225 153.562 25.7625 153.268 25.9431C152.619 26.3446 151.49 25.808 151.437 25.7815C150.124 25.008 149.025 23.987 148.251 22.5905C147.504 21.2464 147.07 19.8044 146.998 18.2653C146.979 17.8938 147.089 17.7623 147.459 17.6948C147.946 17.6048 148.448 17.5858 148.934 17.6573C150.992 17.9578 152.743 18.8773 154.211 20.3344C155.049 21.1639 155.683 22.1554 156.336 23.124C157.03 24.1525 157.777 25.1325 158.729 25.9361C159.065 26.2176 159.333 26.4316 159.589 26.5896C158.816 26.6761 157.525 26.6946 156.642 25.9961V25.9951ZM157.608 19.7814C157.608 19.6164 157.74 19.4849 157.906 19.4849C157.943 19.4849 157.977 19.4924 158.008 19.5034C158.049 19.5184 158.087 19.5409 158.117 19.5749C158.17 19.6274 158.2 19.7024 158.2 19.7814C158.2 19.9464 158.068 20.0779 157.902 20.0779C157.736 20.0779 157.608 19.9464 157.608 19.7814ZM160.608 21.3204C160.416 21.3994 160.223 21.4669 160.038 21.4744C159.751 21.4894 159.438 21.3729 159.268 21.2304C159.004 21.0089 158.815 20.8849 158.736 20.4984C158.702 20.3334 158.721 20.0779 158.751 19.9314C158.819 19.6159 158.744 19.4134 158.521 19.2294C158.34 19.0793 158.11 19.0378 157.857 19.0378C157.762 19.0378 157.676 18.9963 157.611 18.9628C157.506 18.9103 157.419 18.7788 157.502 18.6173C157.528 18.5648 157.657 18.4373 157.687 18.4148C158.03 18.2193 158.426 18.2833 158.793 18.4298C159.132 18.5688 159.389 18.8238 159.759 19.1844C160.136 19.6199 160.204 19.7399 160.419 20.0669C160.589 20.3224 160.744 20.5849 160.85 20.8854C160.914 21.0729 160.831 21.2264 160.608 21.3204Z" fill="var(--dsw-alias-label-primary)"/>
-</g>
-<rect x="274.674" y="13.5" width="52" height="14" rx="2" fill="var(--dsw-alias-label-primary)"/>
-<g clip-path="url(#clip1_2141_5628)">
-<path d="M278.174 16.9328H279.406V24.1378H278.174V16.9328ZM281.826 16.9328H283.058V24.1378H281.826V16.9328ZM278.691 21.0248V19.9908H282.519V21.0248H278.691Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-<path d="M285.723 22.4328L285.998 21.4538H288.528L288.858 22.4328H285.723ZM285.613 24.1378H284.381L286.603 16.9328H287.527L287.472 17.7468L286.273 21.9158H286.295L285.613 24.1378ZM290.365 24.1378H289.067L288.396 21.9488L288.407 21.9378L287.197 17.7468L287.252 16.9328H288.143L290.365 24.1378Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-<path d="M292.172 16.9328H294.393C295.178 16.9328 295.768 17.1161 296.164 17.4828C296.56 17.8421 296.758 18.3334 296.758 18.9568C296.758 19.2208 296.722 19.4664 296.649 19.6938C296.575 19.9211 296.451 20.1264 296.274 20.3098C296.098 20.4931 295.857 20.6508 295.549 20.7828C295.248 20.9074 294.867 21.0064 294.405 21.0798V21.3218H292.172V20.6398L293.349 20.4858C293.957 20.4051 294.416 20.2988 294.724 20.1668C295.032 20.0348 295.24 19.8771 295.351 19.6938C295.461 19.5031 295.516 19.2941 295.516 19.0668C295.516 18.7001 295.409 18.4178 295.197 18.2198C294.984 18.0218 294.65 17.9228 294.195 17.9228H292.172V16.9328ZM291.72 16.9328H292.953V24.1378H291.72V16.9328ZM297.243 24.0938V24.1378H295.691L294.35 22.3228C294.196 22.1101 294.056 21.9414 293.932 21.8168C293.807 21.6848 293.671 21.5894 293.524 21.5308C293.378 21.4648 293.198 21.4208 292.986 21.3988C292.773 21.3694 292.502 21.3511 292.172 21.3438V21.1458H294.405C294.559 21.2118 294.694 21.2851 294.812 21.3658C294.936 21.4464 295.061 21.5454 295.186 21.6628C295.318 21.7801 295.464 21.9378 295.625 22.1358L297.243 24.0938Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-<path d="M298.906 17.5708L298.917 16.9328H299.786L302.91 23.5108V24.1378H302.03L298.906 17.5708ZM303.35 24.1378H302.294L302.206 16.9328H303.35V24.1378ZM299.566 24.1378H298.422V16.9328H299.478L299.566 24.1378Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-<path d="M305.289 16.9328H306.532V24.1378H305.289V16.9328ZM305.421 17.9668V16.9328H310.184V17.9668H305.421ZM305.421 24.1378V23.1038H310.228V24.1378H305.421ZM305.421 21.0138V19.9908H309.7V21.0138H305.421Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-<path d="M314.378 23.2578C314.869 23.2578 315.221 23.1661 315.434 22.9828C315.654 22.7994 315.764 22.5464 315.764 22.2238C315.764 22.0478 315.731 21.8974 315.665 21.7728C315.599 21.6481 315.478 21.5344 315.302 21.4318C315.133 21.3218 314.884 21.2154 314.554 21.1128L313.817 20.8818C313.172 20.6764 312.706 20.4051 312.42 20.0678C312.134 19.7304 311.991 19.3014 311.991 18.7808C311.991 18.4288 312.086 18.1024 312.277 17.8018C312.468 17.5011 312.754 17.2591 313.135 17.0758C313.516 16.8924 313.989 16.8008 314.554 16.8008C314.957 16.8008 315.324 16.8301 315.654 16.8888C315.991 16.9401 316.365 17.0171 316.776 17.1198L316.6 18.1758C316.16 18.0511 315.768 17.9631 315.423 17.9118C315.086 17.8531 314.789 17.8238 314.532 17.8238C314.063 17.8238 313.729 17.9081 313.531 18.0768C313.333 18.2381 313.234 18.4398 313.234 18.6818C313.234 18.8578 313.267 19.0154 313.333 19.1548C313.399 19.2868 313.516 19.4078 313.685 19.5178C313.861 19.6278 314.11 19.7341 314.433 19.8368L315.192 20.0788C315.852 20.2768 316.321 20.5334 316.6 20.8488C316.879 21.1568 317.018 21.5858 317.018 22.1358C317.018 22.5611 316.915 22.9351 316.71 23.2578C316.505 23.5804 316.204 23.8334 315.808 24.0168C315.419 24.2001 314.935 24.2918 314.356 24.2918C313.953 24.2918 313.538 24.2478 313.113 24.1598C312.688 24.0718 312.226 23.9434 311.727 23.7748L311.991 22.7188C312.482 22.8948 312.926 23.0304 313.322 23.1258C313.725 23.2138 314.077 23.2578 314.378 23.2578Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-<path d="M321.135 23.2578C321.626 23.2578 321.978 23.1661 322.191 22.9828C322.411 22.7994 322.521 22.5464 322.521 22.2238C322.521 22.0478 322.488 21.8974 322.422 21.7728C322.356 21.6481 322.235 21.5344 322.059 21.4318C321.89 21.3218 321.641 21.2154 321.311 21.1128L320.574 20.8818C319.929 20.6764 319.463 20.4051 319.177 20.0678C318.891 19.7304 318.748 19.3014 318.748 18.7808C318.748 18.4288 318.843 18.1024 319.034 17.8018C319.225 17.5011 319.511 17.2591 319.892 17.0758C320.273 16.8924 320.746 16.8008 321.311 16.8008C321.714 16.8008 322.081 16.8301 322.411 16.8888C322.748 16.9401 323.122 17.0171 323.533 17.1198L323.357 18.1758C322.917 18.0511 322.525 17.9631 322.18 17.9118C321.843 17.8531 321.546 17.8238 321.289 17.8238C320.82 17.8238 320.486 17.9081 320.288 18.0768C320.09 18.2381 319.991 18.4398 319.991 18.6818C319.991 18.8578 320.024 19.0154 320.09 19.1548C320.156 19.2868 320.273 19.4078 320.442 19.5178C320.618 19.6278 320.867 19.7341 321.19 19.8368L321.949 20.0788C322.609 20.2768 323.078 20.5334 323.357 20.8488C323.636 21.1568 323.775 21.5858 323.775 22.1358C323.775 22.5611 323.672 22.9351 323.467 23.2578C323.262 23.5804 322.961 23.8334 322.565 24.0168C322.176 24.2001 321.692 24.2918 321.113 24.2918C320.71 24.2918 320.295 24.2478 319.87 24.1598C319.445 24.0718 318.983 23.9434 318.484 23.7748L318.748 22.7188C319.239 22.8948 319.683 23.0304 320.079 23.1258C320.482 23.2138 320.834 23.2578 321.135 23.2578Z" fill="var(--dsw-alias-label-primary-inverted)"/>
-</g>
-<defs>
-<clipPath id="clip0_2141_5628">
-<rect width="23.16" height="17.0435" fill="var(--dsw-alias-label-primary-inverted)" transform="translate(145.467 11.5215)"/>
-</clipPath>
-<clipPath id="clip1_2141_5628">
-<rect width="46" height="14" fill="var(--dsw-alias-label-primary-inverted)" transform="translate(277.674 13.5)"/>
-</clipPath>
-</defs>
-</svg>
+<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 472 40" role="img" aria-label="Mio"><g transform="translate(188 -15) scale(.065)"><path fill="#FF6900" d="M150 430C324 376 464 430 512 548C560 430 700 376 874 430C800 520 700 570 598 588C586 640 590 700 612 758C576 788 448 788 412 758C434 700 438 640 426 588C324 570 224 520 150 430Z"/></g><text x="251" y="31" fill="#C85100" font-size="32" font-weight="600" font-family="system-ui,sans-serif">Mio</text></svg>
diff --git a/apps/desktop/renderer/welcome.css b/apps/desktop/renderer/welcome.css
index 3b4da2d..fca69c4 100644
--- a/apps/desktop/renderer/welcome.css
+++ b/apps/desktop/renderer/welcome.css
@@ -341,3 +341,13 @@ body {
 .actions .welcome-loading { color: inherit; }
 
 .auth-waiting .copy-link:disabled { cursor: default; }
+
+/* Mio keeps the upstream window layout and adds the regional connection option. */
+.mio-region { display: flex; align-items: center; justify-content: space-between; gap: 12px; margin-top: 16px; font-size: 13px; }
+.mio-region select { color: inherit; background: transparent; border: 1px solid var(--dsw-alias-border-l4); border-radius: 8px; padding: 6px 10px; }
+
+/* Mio changes accent tokens while keeping the upstream window geometry. */
+body { --dsw-alias-brand-primary: #C85100; --dsw-alias-button-primary-hover: #A64300; }
+@media (prefers-color-scheme: dark) {
+  body { --dsw-alias-brand-primary: #FF9A55; --dsw-alias-button-primary-hover: #FFB680; }
+}
diff --git a/apps/desktop/scripts/desktop-package-environment.mjs b/apps/desktop/scripts/desktop-package-environment.mjs
index 3addf3f..a38e4f7 100644
--- a/apps/desktop/scripts/desktop-package-environment.mjs
+++ b/apps/desktop/scripts/desktop-package-environment.mjs
@@ -1,3 +1,4 @@
+import { mioProduct } from './mio-product.mjs'
 /** Load platform-local release settings without changing the caller's process environment. */
 
 import { accessSync, constants, readFileSync, statSync } from 'node:fs'
@@ -77,13 +78,13 @@ function requireReadableFile(environment, name) {
  * @returns {void}
  */
 export function validateDesktopPackageEnvironment(environment, target, options = {}) {
-  resolveDesktopAppId(environment)
+  if (resolveDesktopAppId(environment) !== mioProduct.appId) throw new Error('Desktop release app ID must match desktop/product.json')
   resolveNpmRegistry(environment)
-  resolveDesktopPolicyEnvironment(environment)
+  if (mioProduct.updateOrigin !== null) resolveDesktopPolicyEnvironment(environment)
   if (target.platform === 'darwin') resolveMacOSPackageSettings(environment)
   else resolveWindowsPackageSettings(environment)
   if (options.unsigned) return
-  if (!options.prepareOnly) resolveDesktopAutoUpdateConfig(environment, target.platform, target.arch)
+  if (!options.prepareOnly && mioProduct.updateOrigin !== null) resolveDesktopAutoUpdateConfig(environment, target.platform, target.arch)
   if (target.platform === 'win32') {
     if (!options.prepareOnly) createWindowsTokenSigner({
       certificateFile: environment.DSH_DESKTOP_WINDOWS_CER_FILE,
diff --git a/apps/desktop/scripts/development-app.ts b/apps/desktop/scripts/development-app.ts
index ff09b8b..97f74ec 100644
--- a/apps/desktop/scripts/development-app.ts
+++ b/apps/desktop/scripts/development-app.ts
@@ -1,7 +1,7 @@
 /** macOS development bundle that loads the current workspace through Electron. */
 import { execFileSync } from 'node:child_process'
 import { createHash } from 'node:crypto'
-import { existsSync, readFileSync, rmSync, writeFileSync } from 'node:fs'
+import { copyFileSync, existsSync, readFileSync, rmSync, writeFileSync } from 'node:fs'
 import { dirname, join } from 'node:path'
 
 /** Workspace and runtime settings captured for Launch Services cold starts. */
@@ -26,25 +26,27 @@ function quote(value: string): string { return `'${value.replaceAll("'", "'\\''"
  */
 export function prepareDevelopmentApp(options: DevelopmentAppOptions): string {
   const source = dirname(dirname(dirname(options.electron)))
-  const bundle = join(options.directory, 'Harness Dev.app')
-  const executable = join(bundle, 'Contents', 'MacOS', 'HarnessDev')
+  const bundle = join(options.directory, 'Mio Dev.app')
+  const executable = join(bundle, 'Contents', 'MacOS', 'MioDev')
   const stamp = join(bundle, 'Contents', 'Resources', 'dsh-development.json')
   const launcher = developmentLauncher(options, bundle)
-  const identity = JSON.stringify({ ...options, launcher, plist: readFileSync(join(source, 'Contents', 'Info.plist'), 'utf8') })
+  const identity = JSON.stringify({ ...options, launcher, icon: createHash('sha256').update(readFileSync(join(options.appRoot, 'resources', 'mio.icns'))).digest('hex'), plist: readFileSync(join(source, 'Contents', 'Info.plist'), 'utf8') })
   if (!existsSync(stamp) || readFileSync(stamp, 'utf8') !== identity) {
     rmSync(bundle, { recursive: true, force: true })
     execFileSync('/usr/bin/ditto', [source, bundle])
     const plist = join(bundle, 'Contents', 'Info.plist')
     const values = {
-      CFBundleIdentifier: `com.deepseek.harness.dev.${createHash('sha256').update(options.appRoot).digest('hex').slice(0, 12)}`,
-      CFBundleName: 'Harness Dev',
-      CFBundleDisplayName: 'Harness Dev',
-      CFBundleExecutable: 'HarnessDev',
-      CFBundleURLTypes: [{ CFBundleURLName: 'DeepSeek Harness', CFBundleURLSchemes: ['dsh'], CFBundleTypeRole: 'Viewer' }],
+      CFBundleIdentifier: `io.github.shin4.mio.dev.${createHash('sha256').update(options.appRoot).digest('hex').slice(0, 12)}`,
+      CFBundleName: 'Mio Dev',
+      CFBundleDisplayName: 'Mio Dev',
+      CFBundleExecutable: 'MioDev',
+      CFBundleIconFile: 'mio.icns',
+      CFBundleURLTypes: [{ CFBundleURLName: 'Mio', CFBundleURLSchemes: ['mio'], CFBundleTypeRole: 'Viewer' }],
     }
     for (const [key, value] of Object.entries(values)) {
       execFileSync('/usr/bin/plutil', ['-replace', key, '-json', JSON.stringify(value), plist])
     }
+    copyFileSync(join(options.appRoot, 'resources', 'mio.icns'), join(bundle, 'Contents', 'Resources', 'mio.icns'))
     writeFileSync(executable, launcher, { mode: 0o755 })
     writeFileSync(stamp, identity, { mode: 0o600 })
     execFileSync('/usr/bin/codesign', ['--force', '--deep', '--sign', '-', bundle], { stdio: 'pipe' })
diff --git a/apps/desktop/scripts/electron-builder-config.mjs b/apps/desktop/scripts/electron-builder-config.mjs
index bab440e..7da1c26 100644
--- a/apps/desktop/scripts/electron-builder-config.mjs
+++ b/apps/desktop/scripts/electron-builder-config.mjs
@@ -1,3 +1,4 @@
+import { mioProduct } from './mio-product.mjs'
 import { officePackageDirectories } from '../../../scripts/libreoffice-packages.mjs'
 import { X509Certificate } from 'node:crypto'
 import { readFileSync } from 'node:fs'
@@ -7,7 +8,6 @@ import { fileURLToPath } from 'node:url'
 import { execFile } from 'node:child_process'
 import { promisify } from 'node:util'
 import {
-  resolveDesktopAppId,
   resolveMacOSNotarizationEnvironment,
   resolveMacOSSigningEnvironment,
 } from './desktop-release-environment.mjs'
@@ -19,7 +19,6 @@ import {
   resolveWindowsUpdatePublisher,
   scrubWindowsSigningEnvironment,
 } from './windows-sign.mjs'
-import { resolveDesktopAutoUpdateConfig } from './desktop-auto-update-environment.mjs'
 import { resolveDesktopBuildCommit } from './desktop-build-commit.mjs'
 import { resolveDesktopBuildVersion } from './desktop-build-version.mjs'
 import { resolveDesktopPolicyEnvironment } from './desktop-policy-environment.mjs'
@@ -50,8 +49,7 @@ export function createElectronBuilderConfig(
   preparedRuntime = undefined,
   preparedRuntimeVersion = undefined,
 ) {
-  const appId = resolveDesktopAppId(env)
-  const policy = resolveDesktopPolicyEnvironment(env)
+  const policy = mioProduct.updateOrigin === null ? undefined : resolveDesktopPolicyEnvironment(env)
   const targetPlatform = env.DSH_DESKTOP_TARGET_PLATFORM
   const resolvedPlatform = targetPlatform ?? hostPlatform
   const resolvedArch = env.DSH_DESKTOP_TARGET_ARCH ?? hostArch
@@ -90,25 +88,28 @@ export function createElectronBuilderConfig(
   if (windowsSigner !== undefined) {
     installWindowsNsisBootstrapSigner({ sign: windowsSigner })
   }
-  const update = unsigned ? undefined : resolveDesktopAutoUpdateConfig(env, resolvedPlatform, resolvedArch)
+  const update = unsigned || mioProduct.updateOrigin === null ? undefined : {
+    publicUrl: `${mioProduct.updateOrigin}/${resolvedPlatform}-${resolvedArch}`,
+  }
   if (preparedRuntime !== undefined) buildPaths.dsh = preparedRuntime
   // electron-builder merges extraMetadata into the packaged manifest, so a build version here reaches
   // the artifact names, the update feed, and the installed app.getVersion() the updater compares against.
-  const productVersion = JSON.parse(readFileSync(fileURLToPath(new URL('../package.json', import.meta.url)), 'utf8')).version
+  const productVersion = mioProduct.version
   const buildVersion = resolveDesktopBuildVersion(env, productVersion)
   const packaged = resolveDesktopBuildCommit(env)
   return {
-    appId,
-    protocols: [{ name: 'DeepSeek Harness', schemes: ['dsh'] }],
+    appId: mioProduct.appId,
+    protocols: [{ name: mioProduct.name, schemes: [mioProduct.protocol] }],
     extraMetadata: {
-      dshDesktopAppId: appId,
+      name: 'mio-desktop',
+      dshDesktopAppId: mioProduct.appId,
       dshMandatoryUpdatePolicy: policy,
-      ...buildVersion === productVersion ? {} : { version: buildVersion },
+      version: buildVersion,
       ...packaged === undefined ? {} : { dshBuildCommit: packaged.commit, dshBuildDirty: packaged.dirty },
     },
-    productName: 'DeepSeek Harness',
+    productName: mioProduct.name,
     // Unsigned builds carry their own suffix so a shared file can never pass for a release artifact.
-    artifactName: `deepseek-harness-\${version}-\${os}-\${arch}${unsigned ? '-unsigned' : ''}.\${ext}`,
+    artifactName: `mio-\${version}-\${os}-\${arch}${unsigned ? '-unsigned' : ''}.\${ext}`,
     directories: { output: unsigned ? buildPaths.unsignedArtifacts : buildPaths.artifacts },
     asar: true,
     electronDist: buildPaths.electron,
@@ -151,11 +152,10 @@ export function createElectronBuilderConfig(
       icon: fileURLToPath(new URL('../resources/icon-macos.png', import.meta.url)),
       category: 'public.app-category.developer-tools',
       // macOS matches the application locale against this bundle, not Electron Framework resources.
-      extendInfo: { CFBundleLocalizations: ['en', 'zh_CN'] },
       identity: macOSSigning?.signingIdentity,
       forceCodeSigning: true,
       hardenedRuntime: true,
-      extendInfo: { NSMicrophoneUsageDescription: 'DeepSeek Harness uses your microphone to transcribe speech into message drafts.' },
+      extendInfo: { CFBundleLocalizations: ['en', 'zh_CN'], NSMicrophoneUsageDescription: 'Mio uses your microphone to transcribe speech into message drafts.' },
       // ASAR-unpacked native runtime files are pre-signed; PAK resources are sealed by their enclosing bundle.
       signIgnore: ['/Contents/Resources/app\\.asar\\.unpacked/dsh(?:/|$)', '/Contents/Resources/runtime/primary-runtime(?:/|$)', '\\.pak$'],
       notarize: true,
@@ -189,7 +189,7 @@ export function createElectronBuilderConfig(
       // The bundled runtime declares whichever version prepared it: the product version for an ordinary
       // release, and a rewritten one for installed-update qualification.
       await verifyDesktopRuntime(buildPaths.dsh,
-        preparedRuntimeVersion ?? productVersion, { platform: resolvedPlatform, arch: resolvedArch })
+        preparedRuntimeVersion ?? JSON.parse(readFileSync(fileURLToPath(new URL('../package.json', import.meta.url)), 'utf8')).version, { platform: resolvedPlatform, arch: resolvedArch })
       // Unsigned Windows builds skip electron-builder's afterSign hook.
       if (packagesWindows && unsigned) await verifyWindowsAsarUnpack(buildPaths.dsh, resourcesDir, windowsCode)
     },
diff --git a/apps/desktop/scripts/package-macos.ts b/apps/desktop/scripts/package-macos.ts
index c1a2cb5..f7173aa 100644
--- a/apps/desktop/scripts/package-macos.ts
+++ b/apps/desktop/scripts/package-macos.ts
@@ -1,5 +1,6 @@
 /** Build the ZIP and DMG from separate signed application copies with overlapping notarization. */
 
+import { mioProduct } from '../src/mio-product.ts'
 import { execFile } from 'node:child_process'
 import { mkdtemp, rename, rm, stat } from 'node:fs/promises'
 import { basename, dirname, join } from 'node:path'
@@ -77,19 +78,19 @@ export async function packageMacOSArtifacts(
   const secrets = Object.entries(environment).filter(([name]) => /KEY|SECRET|TOKEN|PASSWORD|APPLE_ID/iu.test(name)).map(([, value]) => value ?? '')
   const expected = resolveMacOSSigningEnvironment(environment)
   const credentials = resolveMacOSNotarizationEnvironment(environment)
-  const update = resolveDesktopAutoUpdateConfig(environment, 'darwin', arch)
-  const appPath = join(artifactsRoot, arch === 'arm64' ? 'mac-arm64' : 'mac', 'DeepSeek Harness.app')
+  const update = mioProduct.updateOrigin === null ? undefined : resolveDesktopAutoUpdateConfig(environment, 'darwin', arch)
+  const appPath = join(artifactsRoot, arch === 'arm64' ? 'mac-arm64' : 'mac', 'Mio.app')
   const root = await mkdtemp(join(dirname(artifactsRoot), 'notarization-'))
   const zipApp = join(root, 'zip', basename(appPath))
   const dmgApp = join(root, 'dmg', basename(appPath))
   const zipOutput = join(root, 'zip-artifacts')
   const dmgOutput = join(root, 'dmg-artifacts')
   try {
-    await verifyMacOSAppUpdateConfig(appPath, update)
+    if (update) await verifyMacOSAppUpdateConfig(appPath, update)
     await apple.copyApp(appPath, zipApp)
     await apple.copyApp(appPath, dmgApp)
-    await verifyMacOSAppUpdateConfig(zipApp, update)
-    await verifyMacOSAppUpdateConfig(dmgApp, update)
+    if (update) await verifyMacOSAppUpdateConfig(zipApp, update)
+    if (update) await verifyMacOSAppUpdateConfig(dmgApp, update)
     apple.verifySignature(zipApp, expected)
     apple.verifySignature(dmgApp, expected)
     const results = await Promise.allSettled([
@@ -106,16 +107,18 @@ export async function packageMacOSArtifacts(
     if (failures.length > 0) {
       throw new AggregateError(failures.map(result => result.reason), 'desktop macOS packaging: artifact lanes failed')
     }
-    await verifyMacOSAppUpdateConfig(zipApp, update)
-    await verifyMacOSAppUpdateConfig(dmgApp, update)
+    if (update) await verifyMacOSAppUpdateConfig(zipApp, update)
+    if (update) await verifyMacOSAppUpdateConfig(dmgApp, update)
     apple.verifySignature(zipApp, expected)
     apple.verifySignature(dmgApp, expected)
-    const base = `deepseek-harness-${version}-mac-${arch}`
+    const base = `mio-${version}-mac-${arch}`
     const artifacts = [
       [dmgOutput, `${base}.dmg`],
       [zipOutput, `${base}.zip`],
-      [zipOutput, `${base}.zip.blockmap`],
-      [zipOutput, desktopUpdateMetadataFilename(version, 'darwin')],
+      ...(update ? [
+        [zipOutput, `${base}.zip.blockmap`],
+        [zipOutput, desktopUpdateMetadataFilename(version, 'darwin')],
+      ] as const : []),
     ] as const
     for (const [output, filename] of artifacts) {
       const file = join(output, filename)
diff --git a/apps/desktop/scripts/package-target.ts b/apps/desktop/scripts/package-target.ts
index 62f4fcb..cb05f5d 100644
--- a/apps/desktop/scripts/package-target.ts
+++ b/apps/desktop/scripts/package-target.ts
@@ -1,3 +1,4 @@
+import { mioProduct } from '../src/mio-product.ts'
 /** Build one release target with matching Electron and dsh architecture. */
 
 import { spawn } from 'node:child_process'
@@ -6,7 +7,6 @@ import { parseArgs } from 'node:util'
 import { join, resolve } from 'node:path'
 import {
   desktopBuildRecordFilename,
-  resolveDesktopAutoUpdateConfig,
 } from './desktop-auto-update-environment.mjs'
 import { desktopTargetBuildPaths } from './desktop-build-paths.mjs'
 import { packageMacOSArtifacts, type DesktopPrepackagedArtifact } from './package-macos.ts'
@@ -143,17 +143,16 @@ function writeReleaseRecord(
   if (desktopVersion !== dshVersion) {
     throw new Error(`desktop package: desktop version ${desktopVersion} does not match dsh version ${dshVersion}`)
   }
-  const buildVersion = resolveDesktopBuildVersion(environment, dshVersion)
+  const buildVersion = resolveDesktopBuildVersion(environment, mioProduct.version)
   const packaged = resolveDesktopBuildCommit(environment)
-  const update = resolveDesktopAutoUpdateConfig(environment, target.platform, target.arch)
   const recordPath = join(artifactsRoot, desktopBuildRecordFilename(target.name))
   const temporaryPath = `${recordPath}.tmp`
   writeFileSync(temporaryPath, `${JSON.stringify({
     schemaVersion: 1,
     target: target.name,
     version: buildVersion,
-    environment: update.environment,
-    publicUrl: update.publicUrl,
+    environment: 'mio',
+    publicUrl: mioProduct.updateOrigin,
     // Upload reads this to tag the commit a production release was packaged from.
     ...packaged === undefined ? {} : { commit: packaged.commit, dirty: packaged.dirty },
   }, null, 2)}\n`)
@@ -321,6 +320,7 @@ async function resolveRequestedBuildVersion(
   const requested = invocation.requestedBuildVersion
   if (requested === undefined) return productVersion
   if (requested !== AUTOMATIC_BUILD_VERSION) return validateDesktopBuildVersion(requested, productVersion)
+  if (mioProduct.updateOrigin === null) throw new Error('Mio has no update feed for automatic version discovery')
   const paths = desktopTargetBuildPaths(invocation.target.name)
   return suggestDesktopBuildVersion({
     productVersion, target: invocation.target.name, environment,
@@ -333,7 +333,7 @@ async function main(): Promise<void> {
   const invocation = parseDesktopPackageInvocation(process.argv.slice(2))
   const { target } = invocation
   const environment = loadDesktopPackageEnvironment(target.platform)
-  const productVersion = packageVersion(join(APP_ROOT, 'package.json'), 'desktop package')
+  const productVersion = mioProduct.version
   // Release settings come from the target dotenv file alone, so the version this run publishes is an
   // argument; the environment variable below only carries it to the child processes that build.
   const buildVersion = await resolveRequestedBuildVersion(invocation, productVersion, environment)
@@ -447,7 +447,7 @@ export async function packageTarget(
         { cwd: APP_ROOT, env: electronBuilderEnv, timeoutMs: 60_000 })
     })
   }
-  await execute(['run', 'build:official'], buildEnv, REPOSITORY_ROOT)
+  await execute(['run', 'build', '--profile', 'official'], buildEnv, REPOSITORY_ROOT)
   await execute(['run', 'release:pack', '--family', 'dsh', '--out', buildPaths.packedDsh, ...packArguments], buildEnv, REPOSITORY_ROOT)
   await execute([
     '--dir',
@@ -456,6 +456,10 @@ export async function packageTarget(
     '--pack-destination',
     buildPaths.packedDsh,
   ], buildEnv, REPOSITORY_ROOT)
+  await execute(['--dir', 'mio/desktop', 'pack', '--pack-destination', buildPaths.packedDsh], buildEnv, REPOSITORY_ROOT)
+  await execute(['--dir', 'mio/brand', 'pack', '--pack-destination', buildPaths.packedDsh], buildEnv, REPOSITORY_ROOT)
+  await execute(['--dir', 'mio/asr', 'pack', '--pack-destination', buildPaths.packedDsh], buildEnv, REPOSITORY_ROOT)
+  await execute(['--dir', 'mio/tts', 'pack', '--pack-destination', buildPaths.packedDsh], buildEnv, REPOSITORY_ROOT)
   await execute(['run', 'release:pack', '--family', 'vendor', '--out', buildPaths.packedVendor, ...packArguments], buildEnv, REPOSITORY_ROOT)
   rmSync(buildPaths.packedLandlock, { recursive: true, force: true })
   mkdirSync(buildPaths.packedLandlock, { recursive: true })
@@ -482,14 +486,14 @@ export async function packageTarget(
     await withMacOSNotarizationProxy(mac?.notarizationProxy, () => packageMacOSArtifacts({
       arch: target.arch,
       // electron-builder named these artifacts after the published version, so locating them uses the same identifier.
-      version: resolveDesktopBuildVersion(environment, packageVersion(join(APP_ROOT, 'package.json'), 'desktop package')),
+      version: resolveDesktopBuildVersion(environment, mioProduct.version),
       artifactsRoot: buildPaths.artifacts,
       environment: electronBuilderEnv,
     }, artifact => execute(desktopElectronBuilderArguments(target, false, artifact), electronBuilderEnv)), undefined, undefined, proxyEvent)
   } else if (target.platform === 'darwin') {
     await execute([...desktopElectronBuilderArguments(target, true), '--config.mac.notarize=false'], electronBuilderEnv)
     await execute(['exec', 'tsx', 'scripts/smoke-packaged-runtime.ts'], targetEnv)
-    const appPath = join(buildPaths.artifacts, target.arch === 'arm64' ? 'mac-arm64' : 'mac', 'DeepSeek Harness.app')
+    const appPath = join(buildPaths.artifacts, target.arch === 'arm64' ? 'mac-arm64' : 'mac', 'Mio.app')
     await withMacOSNotarizationProxy(mac?.notarizationProxy,
       () => notarizeMacOS({ appPath, ...resolveMacOSNotarizationEnvironment(environment) }), undefined, undefined, proxyEvent)
   } else {
diff --git a/apps/desktop/scripts/smoke-packaged-runtime.ts b/apps/desktop/scripts/smoke-packaged-runtime.ts
index 106830b..b30e6ed 100644
--- a/apps/desktop/scripts/smoke-packaged-runtime.ts
+++ b/apps/desktop/scripts/smoke-packaged-runtime.ts
@@ -1,5 +1,7 @@
 /** Validate the assembled application, including native Office conversion outside ASAR. */
 import { join } from 'node:path'
+import { cp, mkdtemp, realpath, rm } from 'node:fs/promises'
+import { tmpdir } from 'node:os'
 import { parseArgs } from 'node:util'
 import { resolveDesktopBuildTarget, resolveDesktopTargetBuildPaths } from './desktop-build-paths.mjs'
 import { readDesktopRuntime, verifyDesktopRuntime } from '../src/runtime-tree.ts'
@@ -14,10 +16,27 @@ const windows = target === 'win-x64'
 if (values.unsigned && !windows) throw new Error('desktop smoke: unsigned artifacts require Windows')
 const artifacts = values.unsigned ? paths.unsignedArtifacts : paths.artifacts
 const application = windows ? join(artifacts, 'win-unpacked')
-  : join(artifacts, target === 'mac-arm64' ? 'mac-arm64' : 'mac', 'DeepSeek Harness.app', 'Contents')
+  : join(artifacts, target === 'mac-arm64' ? 'mac-arm64' : 'mac', 'Mio.app', 'Contents')
 const resources = join(application, windows ? 'resources' : 'Resources')
-const executable = windows ? join(application, 'DeepSeek Harness.exe') : join(application, 'MacOS', 'DeepSeek Harness')
+const executable = windows ? join(application, 'Mio.exe') : join(application, 'MacOS', 'Mio')
 const descriptor = await verifyDesktopRuntime(paths.dsh, readDesktopRuntime(paths.dsh).release.version,
   resolveDesktopPackageTarget(target))
 if (windows && !values.unsigned) await verifyWindowsCode(application)
-await smokePreparedRuntime(join(resources, 'app.asar', 'dsh'), executable, join(resources, 'runtime'), descriptor)
+try {
+  await smokePreparedRuntime(join(resources, 'app.asar', 'dsh'), executable, join(resources, 'runtime'), descriptor)
+} catch (error) {
+  if (!windows || !(error instanceof Error) || !error.message.includes('LibreOffice native conversion failed')) throw error
+  // Native Office resources can exceed Windows path limits inside the nested CI output tree.
+  // Retry the identical assembled payload in a normal-length directory; never skip its checks.
+  console.error('Packaged smoke failed in the build directory; verifying an identical relocated payload:', error)
+  const temporary = await realpath(await mkdtemp(join(tmpdir(), 'mio-smoke-')))
+  try {
+    const relocated = join(temporary, 'Mio')
+    await cp(application, relocated, { recursive: true })
+    await smokePreparedRuntime(join(relocated, 'resources', 'app.asar', 'dsh'),
+      join(relocated, 'Mio.exe'), join(relocated, 'resources', 'runtime'), descriptor)
+    console.log('Identical Windows payload passed after relocation out of the nested build directory')
+  } finally {
+    await rm(temporary, { recursive: true, force: true })
+  }
+}
diff --git a/apps/desktop/scripts/smoke-runtime.ts b/apps/desktop/scripts/smoke-runtime.ts
index f15f10a..8af75b8 100644
--- a/apps/desktop/scripts/smoke-runtime.ts
+++ b/apps/desktop/scripts/smoke-runtime.ts
@@ -1,6 +1,6 @@
 /** Boot the materialized target runtime without access to a user's Harness profile. */
 
-import { mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from 'node:fs'
+import { mkdirSync, mkdtempSync, readFileSync, realpathSync, rmSync, writeFileSync } from 'node:fs'
 import { tmpdir } from 'node:os'
 import { join } from 'node:path'
 import { execFile } from 'node:child_process'
@@ -23,7 +23,8 @@ import type { DesktopRuntimeDescriptor } from '../src/runtime-tree.ts'
 export async function smokeDesktopRuntime(
   root: string, node: string, runtime: DesktopRuntimeDescriptor, environment: NodeJS.ProcessEnv, resourcesRuntime: string,
 ): Promise<void> {
-  const home = mkdtempSync(join(tmpdir(), 'dsh-desktop-smoke-'))
+  // Canonicalize Windows TEMP aliases before creating the external plugin profile.
+  const home = realpathSync(mkdtempSync(join(tmpdir(), 'dsh-desktop-smoke-')))
   const profile = join(home, 'profiles', 'desktop')
   const host = new DesktopHostProcess(node, root, profile, undefined, { ...environment, DSH_HOME: home },
     undefined, join(resourcesRuntime, 'primary-runtime'),
@@ -110,7 +111,20 @@ export function apply(ctx) {
       throw new Error('desktop runtime: packaged frontend smoke failed')
     }
     const pluginResponse = await fetch(new URL('/desktop-smoke', ready.url), { headers: { cookie } })
-    if (await pluginResponse.text() !== 'plugin route ready') throw new Error('desktop runtime: plugin HTTP route failed')
+    const pluginBody = await pluginResponse.text()
+    if (pluginBody !== 'plugin route ready') {
+      // Emit only fixed diagnostic categories, never arbitrary plugin stderr or credentials.
+      const diagnostic = String(Reflect.get(host, 'stderr'))
+      const flags = {
+        testBundleSkipped: diagnostic.includes('skipping profile bundle "desktop-runtime-smoke-plugin"'),
+        bundleMissing: diagnostic.includes('cannot resolve profile bundle'),
+        moduleMissing: diagnostic.includes('Cannot find module') || diagnostic.includes('ERR_MODULE_NOT_FOUND'),
+        fileMissing: diagnostic.includes('ENOENT'),
+        incompatiblePeer: diagnostic.includes('is incompatible with dsh'),
+        duplicateCordis: diagnostic.includes('external plugin loaded another Cordis instance'),
+      }
+      throw new Error(`desktop runtime: plugin HTTP route failed (${pluginResponse.status}); ${JSON.stringify(flags)}`)
+    }
     for (const { extension } of inputs) {
       const converted = await fetch(new URL(`/desktop-smoke-office/${extension}`, ready.url), {
         headers: { cookie }, signal: AbortSignal.timeout(120_000),
diff --git a/apps/desktop/src/client/WelcomePage.tsx b/apps/desktop/src/client/WelcomePage.tsx
index 8fcde77..5cae541 100644
--- a/apps/desktop/src/client/WelcomePage.tsx
+++ b/apps/desktop/src/client/WelcomePage.tsx
@@ -26,6 +26,7 @@ export function Welcome({ api }: { api: WelcomeApi }) {
   const [copyFeedback, setCopyFeedback] = useState<{ status: 'idle' | 'busy' | 'copied' | 'failed' }>({ status: 'idle' })
   const copyState = copyFeedback.status
   const [draft, setDraft] = useState('')
+  const [region, setRegion] = useState('cn')
   const [error, setError] = useState('')
   const [busy, setBusy] = useState(false)
   const busyRef = useRef(false)
@@ -96,7 +97,7 @@ export function Welcome({ api }: { api: WelcomeApi }) {
     setBusy(true)
     setError('')
     try {
-      const result = await api.saveApiKey(value)
+      const result = await api.saveApiKey(value, region)
       if (!mounted.current) return
       if (result.ok) setDraft('')
       else setError(m.welcomeKeyFailed)
@@ -121,19 +122,7 @@ export function Welcome({ api }: { api: WelcomeApi }) {
       if (mounted.current) setBusy(false)
     }
   }
-  async function start() {
-    navigate('account')
-    setStarting(true)
-    setAttempt(null)
-    attemptRef.current = null
-    const current = ++revision.current
-    try {
-      const state = await api.startSignIn()
-      if (mounted.current && revision.current === current) showAccount(state)
-    } catch {
-      if (mounted.current && revision.current === current) setStarting(false)
-    }
-  }
+  async function start() { navigate('key') }
   async function cancel() {
     if (cancelling || attemptRef.current === null) return
     setCancelling(true)
@@ -183,6 +172,13 @@ export function Welcome({ api }: { api: WelcomeApi }) {
           <input ref={input} id="key-input" type="password" autoComplete="new-password" autoCapitalize="off" spellCheck={false} required
             aria-describedby="key-description key-error" aria-invalid={error !== ''} placeholder={m.welcomeKeyPlaceholder}
             value={draft} disabled={busy} onChange={(event) => { setDraft(event.target.value); setError('') }} />
+          {draft.trim().startsWith('tp-') && <label className="mio-region">{m.welcomeRegion}
+            <select value={region} disabled={busy} onChange={event => { setRegion(event.target.value); setError('') }}>
+              <option value="cn">{m.welcomeRegionCn}</option>
+              <option value="sgp">{m.welcomeRegionSgp}</option>
+              <option value="ams">{m.welcomeRegionAms}</option>
+            </select>
+          </label>}
           <p id="key-error" className="key-error" role="alert" hidden={error === ''}>{error}</p>
         </div>
       </form>
@@ -199,14 +195,14 @@ export function Welcome({ api }: { api: WelcomeApi }) {
           <StateDot state="ongoing" size={16} className="welcome-loading" />
         </button>
         <button id="auth-retry" className="primary" type="button" hidden={!failed} onClick={() => { void start() }}>{m.welcomeAuthRetry}</button>
-        <button id="auth-api-key" className="secondary" type="button" hidden={!failed} onClick={() => { navigate('key') }}>{m.welcomeApiKey}</button>
+        <button id="auth-api-key" className="secondary" type="button" hidden={!failed} onClick={() => { void skip() }}>{m.welcomeKeyLater}</button>
         <button id="auth-cancel" className="secondary" type="button" hidden={failed}
           disabled={cancelling || phase === 'committing' || phase === 'succeeded' || (phase === 'initializing' && !attempt?.id)}
           onClick={() => { void cancel() }}>{m.welcomeAuthCancel}</button>
       </div>
       <div id="entry-actions" className="actions" hidden={page !== 'entry'}>
         <button id="sign-in" className="primary" type="button" onClick={() => { void start() }}>{m.welcomeSignIn}</button>
-        <button ref={keyButton} id="api-key" className="secondary" type="button" onClick={() => { navigate('key') }}>{m.welcomeApiKey}</button>
+        <button ref={keyButton} id="api-key" className="secondary" type="button" onClick={() => { void skip() }}>{m.welcomeKeyLater}</button>
       </div>
       <div id="key-actions" className="actions" hidden={page !== 'key'}>
         <button id="save-key" className="primary" type="submit" form="key-form" disabled={busy || draft.trim() === ''}>{m.welcomeKeySave}</button>
diff --git a/apps/desktop/src/locale.ts b/apps/desktop/src/locale.ts
index b3f9e53..feab55e 100644
--- a/apps/desktop/src/locale.ts
+++ b/apps/desktop/src/locale.ts
@@ -4,17 +4,17 @@ export const en = {
   application: 'Application',
   fileMenu: 'File',
   closePage: 'Close Page or Window',
-  aboutMenu: 'About DeepSeek Harness',
-  aboutProduct: 'DeepSeek Harness',
+  aboutMenu: 'About Mio',
+  aboutProduct: 'Mio',
   aboutVersion: 'Version V{version}',
-  hideApplication: 'Hide DeepSeek Harness',
+  hideApplication: 'Hide Mio',
   hideOtherApplications: 'Hide Others',
   showAllApplications: 'Show All',
-  quitApplication: 'Quit DeepSeek Harness',
-  openApplication: 'Open DeepSeek Harness',
+  quitApplication: 'Quit Mio',
+  openApplication: 'Open Mio',
   quit: 'Quit',
   cancel: 'Cancel',
-  quitTitle: 'Quit DeepSeek Harness?',
+  quitTitle: 'Quit Mio?',
   quitActiveTasks: 'Running tasks will be interrupted.',
   quitScheduledTasks: 'Scheduled tasks will not run while the app is closed.',
   quitActiveAndScheduledTasks: 'Running tasks will be interrupted, and scheduled tasks will not run while the app is closed.',
@@ -29,7 +29,7 @@ export const en = {
   copy: 'Copy',
   paste: 'Paste',
   selectAll: 'Select All',
-  startupFailed: 'DeepSeek Harness is unavailable',
+  startupFailed: 'Mio is unavailable',
   fatalSummary: 'The application could not start or stopped unexpectedly.',
   startupAddressInUse: 'Another DSH instance (such as dsh web or the desktop app) is running. They cannot start at the same time. Quit the other running DSH instance, then restart.',
   diagnosticTruncated: '… Error details shortened.',
@@ -39,12 +39,12 @@ export const en = {
   restartApplication: 'Restart',
   recoveryOperationFailed: 'The recovery operation failed',
   disableThirdPartyPlugins: 'Disable third-party plugins, back up profile patch, and restart',
-  welcomeTitle: 'DeepSeek Harness',
-  welcomeBrand: 'DeepSeek Harness',
+  welcomeTitle: 'Mio',
+  welcomeBrand: 'Mio',
   welcomeTaglineBefore: 'Welcome to ',
-  welcomeTaglineBrand: 'DeepSeek Harness',
+  welcomeTaglineBrand: 'Mio',
   welcomeTaglineAfter: '',
-  welcomeDescription: 'Build potential. Explore intelligence.',
+  welcomeDescription: 'Create, explore and code with MiMo.',
   welcomeAuthStarting: 'Opening sign in…',
   welcomeAuthWaiting: 'Browser didn’t open automatically?',
   welcomeAuthWaitingDescription: 'Copy the sign-in link and open it in your browser to sign in.',
@@ -57,18 +57,22 @@ export const en = {
   welcomeAuthCopyFailed: 'Could not copy. Try again.',
   welcomeAuthCancel: 'Cancel',
   welcomeAuthRetry: 'Sign in again',
-  welcomeSignIn: 'Sign in',
+  welcomeSignIn: 'Connect MiMo',
   welcomeApiKey: 'Add API Key',
+  welcomeRegion: 'Token Plan region',
+  welcomeRegionCn: 'China',
+  welcomeRegionSgp: 'Singapore',
+  welcomeRegionAms: 'Amsterdam',
   welcomeKeyTitle: 'Add an API key to get started',
-  welcomeKeyDescription: 'Configure official DeepSeek models to start using Harness',
+  welcomeKeyDescription: 'Connect your MiMo account to start coding',
   welcomeKeyPlaceholder: 'Enter API key',
   welcomeKeySave: 'Save and continue',
   welcomeKeyLater: 'Set up later',
-  welcomeKeyBack: 'Back to sign in',
+  welcomeKeyBack: 'Back',
   welcomeSessionExpired: 'You have signed out of your account, please log in again.',
   welcomeKeyBlank: 'Enter an API key.',
   welcomeKeyInvalid: 'Enter the API key itself, without quotes, spaces, or an environment-variable assignment.',
-  welcomeKeyFailed: 'Could not save the API key. Please try again.',
+  welcomeKeyFailed: 'Could not verify or save the key. Check the key and region, then try again.',
   welcomeContinueFailed: 'Could not open the workspace. Please try again.',
   checkUpdatesMenu: 'Check for Updates…',
   reloadPageMenu: 'Reload Page',
@@ -85,7 +89,7 @@ export const en = {
   updateCurrent: 'No updates available. Current version: V{version}',
   updateChecking: 'Checking for updates…',
   updateDownload: 'Download update',
-  updateDownloadedTitle: 'DeepSeek Harness v{version} downloaded',
+  updateDownloadedTitle: 'Mio v{version} downloaded',
   updateDownloadedDetail: 'The update package has downloaded. Select “Install and Restart” to restart the app and begin installation.',
   updateDownloadedTitleWindows: 'New version v{version} is ready',
   updateDownloadedDetailWindows: 'The app will close temporarily during the update and reopen automatically when it is complete.\n\nThe update may take some time. Please wait and do not launch the app again during installation.',
@@ -103,9 +107,9 @@ export const en = {
   updateTasksUnavailable: 'Task status is unavailable. Try updating again when the workspace is ready.',
   updateStopFailed: 'Tasks could not be stopped safely. The update was not installed. Please try again later.',
   updateTechnicalDetails: 'View technical details',
-  updateTitle: 'DeepSeek Harness Update',
+  updateTitle: 'Mio Update',
   updateAvailable: 'An update is available',
-  updateDetail: 'DeepSeek Harness {version}\n\nDeepSeek Harness will restart to complete the update.',
+  updateDetail: 'Mio {version}\n\nMio will restart to complete the update.',
   installAndRestart: 'Install and Restart',
   later: 'Later',
   updateFailedTitle: 'Update Failed',
@@ -148,17 +152,17 @@ export const zh = {
   application: '应用',
   fileMenu: '文件',
   closePage: '关闭页面或窗口',
-  aboutMenu: '关于 DeepSeek Harness',
-  aboutProduct: 'DeepSeek Harness',
+  aboutMenu: '关于 Mio',
+  aboutProduct: 'Mio',
   aboutVersion: '版本 V{version}',
-  hideApplication: '隐藏 DeepSeek Harness',
+  hideApplication: '隐藏 Mio',
   hideOtherApplications: '隐藏其他',
   showAllApplications: '显示全部',
-  quitApplication: '退出 DeepSeek Harness',
-  openApplication: '打开 DeepSeek Harness',
+  quitApplication: '退出 Mio',
+  openApplication: '打开 Mio',
   quit: '退出',
   cancel: '取消',
-  quitTitle: '退出 DeepSeek Harness？',
+  quitTitle: '退出 Mio？',
   quitActiveTasks: '当前正在运行的任务将会中断',
   quitScheduledTasks: '应用关闭期间，定时任务不会运行',
   quitActiveAndScheduledTasks: '当前正在运行的任务将会中断，且应用关闭期间，定时任务不会运行',
@@ -173,7 +177,7 @@ export const zh = {
   copy: '复制',
   paste: '粘贴',
   selectAll: '全选',
-  startupFailed: 'DeepSeek Harness 无法使用',
+  startupFailed: 'Mio 无法使用',
   fatalSummary: '应用无法启动或已意外停止。',
   startupAddressInUse: '有其他正在运行的 DSH（如其他 dsh web、桌面端），无法同时启动，请退出其他正在运行的 DSH 后重启。',
   diagnosticTruncated: '… 错误详情已截短。',
@@ -183,12 +187,12 @@ export const zh = {
   restartApplication: '重启',
   recoveryOperationFailed: '恢复操作失败',
   disableThirdPartyPlugins: '禁用第三方插件、备份 profile patch 并重启',
-  welcomeTitle: 'DeepSeek Harness',
-  welcomeBrand: 'DeepSeek Harness',
+  welcomeTitle: 'Mio',
+  welcomeBrand: 'Mio',
   welcomeTaglineBefore: '欢迎使用 ',
-  welcomeTaglineBrand: 'DeepSeek Harness',
+  welcomeTaglineBrand: 'Mio',
   welcomeTaglineAfter: '',
-  welcomeDescription: '组装无限可能，共探智能上限',
+  welcomeDescription: '与 MiMo 一起探索、创作和编程',
   welcomeAuthStarting: '正在打开登录…',
   welcomeAuthWaiting: '没有自动打开浏览器？',
   welcomeAuthWaitingDescription: '复制登录链接，用浏览器手动打开完成登录',
@@ -201,18 +205,22 @@ export const zh = {
   welcomeAuthCopyFailed: '复制失败，请重试',
   welcomeAuthCancel: '取消',
   welcomeAuthRetry: '重新登录',
-  welcomeSignIn: '登录',
+  welcomeSignIn: '连接 MiMo',
   welcomeApiKey: '添加 API Key',
+  welcomeRegion: 'Token Plan 区域',
+  welcomeRegionCn: '中国',
+  welcomeRegionSgp: '新加坡',
+  welcomeRegionAms: '阿姆斯特丹',
   welcomeKeyTitle: '添加一个 API Key 开始使用',
-  welcomeKeyDescription: '配置 DeepSeek 官方模型，即可开始使用',
+  welcomeKeyDescription: '配置 MiMo，即可开始编码',
   welcomeKeyPlaceholder: '输入 API 密钥',
   welcomeKeySave: '保存并继续',
   welcomeKeyLater: '稍后配置',
-  welcomeKeyBack: '返回登录',
+  welcomeKeyBack: '返回',
   welcomeSessionExpired: '登录信息已失效，请重新登录',
   welcomeKeyBlank: '请输入 API 密钥。',
   welcomeKeyInvalid: '请仅输入 API 密钥，不要包含引号、空格或环境变量赋值。',
-  welcomeKeyFailed: '无法保存 API 密钥，请重试。',
+  welcomeKeyFailed: '无法验证或保存密钥，请检查密钥和区域后重试。',
   welcomeContinueFailed: '无法打开工作区，请重试。',
   checkUpdatesMenu: '检查更新…',
   reloadPageMenu: '刷新页面',
@@ -229,7 +237,7 @@ export const zh = {
   updateCurrent: '当前暂无可用更新。当前版本：V{version}',
   updateChecking: '正在检查更新…',
   updateDownload: '下载更新',
-  updateDownloadedTitle: 'DeepSeek Harness v{version} 下载完成',
+  updateDownloadedTitle: 'Mio v{version} 下载完成',
   updateDownloadedDetail: '安装包已下载完毕，点击“安装并重启”，即刻重启客户端，开始部署。',
   updateDownloadedTitleWindows: '新版本 v{version} 已准备就绪',
   updateDownloadedDetailWindows: '更新期间应用将暂时关闭，完成后会自动打开。\n\n更新可能需要一些时间，请耐心等待，期间请勿重复启动应用。',
@@ -247,9 +255,9 @@ export const zh = {
   updateTasksUnavailable: '无法确认任务状态，请在工作区就绪后重试更新。',
   updateStopFailed: '未能安全停止任务，更新未安装。请稍后重试。',
   updateTechnicalDetails: '查看技术详情',
-  updateTitle: 'DeepSeek Harness 更新',
+  updateTitle: 'Mio 更新',
   updateAvailable: '发现可用更新',
-  updateDetail: 'DeepSeek Harness {version}\n\nDeepSeek Harness 将重启以完成更新。',
+  updateDetail: 'Mio {version}\n\nMio 将重启以完成更新。',
   installAndRestart: '安装并重启',
   later: '稍后',
   updateFailedTitle: '更新失败',
diff --git a/apps/desktop/src/main.ts b/apps/desktop/src/main.ts
index a59a8a8..913cdb7 100644
--- a/apps/desktop/src/main.ts
+++ b/apps/desktop/src/main.ts
@@ -1,3 +1,4 @@
+import { mioProduct } from './mio-product.ts'
 import { WINDOWS_TITLEBAR_HEIGHT } from './windows-layout.ts'
 /** Electron shell: desktop project ownership, custom protocol, windows, and lifecycle. */
 
@@ -76,6 +77,8 @@ const rendererConsole = new RendererConsoleTail()
 
 // Platform-conventional logs directory (macOS ~/Library/Logs/<name>, otherwise under userData);
 // set before ready so the first fatal report already resolves under it.
+app.setName(mioProduct.name)
+if (app.isPackaged) process.env.DSH_HOME = process.env.MIO_HOME ?? join(app.getPath('userData'), mioProduct.dataDirectory)
 app.setAppLogsPath()
 
 function currentDesktopLocale(): ReturnType<typeof resolveDesktopLocale> {
@@ -874,7 +877,7 @@ async function main(): Promise<void> {
   const applicationIconPath = development ? join(app.getAppPath(), 'resources', 'icon-windows.png')
     : join(process.resourcesPath, 'icon.png')
   app.setAboutPanelOptions({
-    applicationName: 'DeepSeek Harness',
+    applicationName: mioProduct.name,
     applicationVersion: app.getVersion(),
     // The release has no separate build number; omit Electron's bundle version.
     version: '',
@@ -1109,9 +1112,9 @@ async function main(): Promise<void> {
           }
           await clipboard.writeText(platformLoginUrl(state.attempt.authorizeUrl))
         },
-        saveApiKey: async (apiKey) => {
+        saveApiKey: async (apiKey, region) => {
           if (backend.host === undefined || welcomeBackend === undefined) return { ok: false }
-          const saved = await welcomeBackend.save(apiKey)
+          const saved = await welcomeBackend.save(apiKey, region)
           if (!saved.ok) return saved
           await enterWorkspace()
           return { ok: true }
@@ -1169,10 +1172,10 @@ async function main(): Promise<void> {
     window.focus()
   }
 
-  if (app.isPackaged || process.env.DSH_DESKTOP_DEV_APP === '1') app.setAsDefaultProtocolClient('dsh')
+  if (app.isPackaged || process.env.DSH_DESKTOP_DEV_APP === '1') app.setAsDefaultProtocolClient(mioProduct.protocol)
   app.on('open-url', (event, url) => {
     event.preventDefault()
-    if (url === 'dsh://open' || url === 'dsh://open/') focusPrimaryWindow()
+    if (url === `${mioProduct.protocol}://open` || url === `${mioProduct.protocol}://open/`) focusPrimaryWindow()
   })
 
   app.on('activate', (_event, hasVisibleWindows) => {
diff --git a/apps/desktop/src/preload-welcome.ts b/apps/desktop/src/preload-welcome.ts
index e9ec1aa..44065de 100644
--- a/apps/desktop/src/preload-welcome.ts
+++ b/apps/desktop/src/preload-welcome.ts
@@ -19,7 +19,7 @@ const api: WelcomeApi = {
     ipcRenderer.on(WELCOME_IPC.state, receive)
     return () => { ipcRenderer.removeListener(WELCOME_IPC.state, receive) }
   },
-  saveApiKey: (value: string) => ipcRenderer.invoke(WELCOME_IPC.saveApiKey, value) as Promise<WelcomeSaveResult>,
+  saveApiKey: (value: string, region?: string) => ipcRenderer.invoke(WELCOME_IPC.saveApiKey, value, region) as Promise<WelcomeSaveResult>,
   skip: () => ipcRenderer.invoke(WELCOME_IPC.skip) as Promise<void>,
 }
 contextBridge.exposeInMainWorld('dshWelcome', api)
diff --git a/apps/desktop/src/project-manager.ts b/apps/desktop/src/project-manager.ts
index 270d87d..80979e6 100644
--- a/apps/desktop/src/project-manager.ts
+++ b/apps/desktop/src/project-manager.ts
@@ -29,7 +29,7 @@ import {
 const PROJECT_NAME = '@deepseek-ai/dsh-desktop-runtime'
 const DSH_PACKAGE = '@deepseek-ai/dsh'
 const CORE_BUILD_PACKAGE = '@deepseek-ai/dsh-subprocess-local'
-const WEB_PROFILE = PROFILE_TEMPLATES.web as ProfileTemplate
+const WEB_PROFILE: ProfileTemplate = { ...PROFILE_TEMPLATES.web, bundles: [...(PROFILE_TEMPLATES.web as ProfileTemplate).bundles, '@deepseek-ai/dsh-experimental-voice-input-bundle', '@mio/desktop'] }
 const WORKSPACE_SETTINGS = 'nodeLinker: hoisted\nautoInstallPeers: false\n'
 function writeJson(path: string, value: unknown): void {
   writeFileSync(path, `${JSON.stringify(value, undefined, 2)}\n`, { mode: 0o600 })
diff --git a/apps/desktop/src/update-coordinator.ts b/apps/desktop/src/update-coordinator.ts
index c7bf91c..70b622a 100644
--- a/apps/desktop/src/update-coordinator.ts
+++ b/apps/desktop/src/update-coordinator.ts
@@ -1,3 +1,4 @@
+import { mioProduct } from './mio-product.ts'
 /** User-authorized downloads and separate installation of one version-bound Desktop release. */
 
 import { existsSync } from 'node:fs'
@@ -50,7 +51,7 @@ export class DesktopUpdateCoordinator {
     private readonly publish: (state: DesktopUpdateState) => DesktopUpdateState,
     private readonly beforeRestart: () => Promise<boolean>,
     private readonly updater: AppUpdater = autoUpdater,
-    private readonly enabled: () => boolean = () => app.isPackaged && existsSync(join(process.resourcesPath, 'app-update.yml')),
+    private readonly enabled: () => boolean = () => mioProduct.updateOrigin !== null && app.isPackaged && existsSync(join(process.resourcesPath, 'app-update.yml')),
     private readonly currentVersion: () => string = () => app.getVersion(),
   ) {
     if (updater === autoUpdater) {
diff --git a/apps/desktop/src/welcome-api.ts b/apps/desktop/src/welcome-api.ts
index 0152967..d235963 100644
--- a/apps/desktop/src/welcome-api.ts
+++ b/apps/desktop/src/welcome-api.ts
@@ -36,7 +36,7 @@ export interface WelcomeOperations {
    * @param value - validated, trimmed API key.
    * @returns whether the write completed, without private error details.
    */
-  saveApiKey(value: string): Promise<WelcomeSaveResult>
+  saveApiKey(value: string, region?: string): Promise<WelcomeSaveResult>
   /**
    * Enter the workspace without writing an onboarding-completion setting.
    * @returns completion after the workspace opens.
diff --git a/apps/desktop/src/welcome-backend.ts b/apps/desktop/src/welcome-backend.ts
index a0bb4e9..40d8b9b 100644
--- a/apps/desktop/src/welcome-backend.ts
+++ b/apps/desktop/src/welcome-backend.ts
@@ -1,3 +1,4 @@
+import { mioProduct } from './mio-product.ts'
 /** Native welcome operations using the shared Web authentication and RPC APIs. */
 
 import { randomUUID } from 'node:crypto'
@@ -19,10 +20,10 @@ export interface DesktopWelcomeBackend {
   /** @returns The saved UI language without account or provider requests. */
   readLocalePreference(): Promise<string | null>
   /**
-   * @param apiKey - User-entered official provider key.
+   * @param apiKey - User-entered MiMo provider key.
    * @returns A safe write outcome without provider diagnostics.
    */
-  save(apiKey: string): Promise<{ ok: boolean }>
+  save(apiKey: string, region?: string): Promise<{ ok: boolean }>
 }
 
 function record(value: unknown): value is Record<string, unknown> {
@@ -64,12 +65,13 @@ export async function connectDesktopWelcome(
   const settingsAndReference = async () => {
     const settings = await invoke({ namespace: 'settings', method: 'describe', args: {} })
     if (!record(settings) || !Array.isArray(settings.namespaces)) throw new Error('desktop welcome: missing settings namespaces')
-    const official: unknown = settings.namespaces.find((item: unknown) => record(item) && item.ns === 'llm-deepseek')
-    if (official === undefined) return { settings: { namespaces: settings.namespaces }, ref: undefined }
-    if (!record(official) || !record(official.value) || typeof official.value.apiKeyEnv !== 'string') {
-      throw new Error('desktop welcome: missing official DeepSeek credential reference')
+    const namespace = settings.namespaces.find((item: unknown) => record(item) && item.ns === 'llm-pi-ai')
+    const providers = record(namespace) && record(namespace.value) ? namespace.value.providers : undefined
+    const provider = record(providers) ? providers[mioProduct.provider] : undefined
+    if (!record(provider) || typeof provider.apiKeyEnv !== 'string') {
+      throw new Error('desktop welcome: missing MiMo credential reference')
     }
-    return { settings: { namespaces: settings.namespaces }, ref: official.value.apiKeyEnv }
+    return { settings: { namespaces: settings.namespaces }, ref: provider.apiKeyEnv }
   }
   const localePreference = (namespaces: unknown[]): string | null => {
     const locale: unknown = namespaces.find((item: unknown) => record(item) && item.ns === 'locale')
@@ -120,11 +122,24 @@ export async function connectDesktopWelcome(
       if (!record(settings) || !Array.isArray(settings.namespaces)) throw new Error('desktop welcome: missing settings namespaces')
       return localePreference(settings.namespaces)
     },
-    async save(apiKey) {
+    async save(apiKey, region = 'cn') {
       if (!/^[\x21-\x7e]+$/.test(apiKey)) return { ok: false }
+      if (!['cn', 'sgp', 'ams'].includes(region)) return { ok: false }
       try {
         const { ref } = await settingsAndReference()
         if (ref === undefined) return { ok: false }
+        const baseURL = apiKey.startsWith('tp-')
+          ? `https://token-plan-${region}.xiaomimimo.com/v1` : 'https://api.xiaomimimo.com/v1'
+        await invoke({ namespace: 'llm', method: 'discoverModels', args: {
+          settingsNs: 'llm-pi-ai', request: { baseURL, api: 'openai-completions', apiKey },
+        } })
+        const { settings } = await settingsAndReference()
+        const namespace = settings.namespaces.find((item: unknown) => record(item) && item.ns === 'llm-pi-ai')
+        if (!record(namespace) || typeof namespace.revision !== 'number') return { ok: false }
+        await invoke({ namespace: 'settings', method: 'mutate', args: {
+          ns: 'llm-pi-ai', ops: [{ op: 'set', path: ['providers', mioProduct.provider, 'baseURL'], value: baseURL }],
+          expectedRevision: namespace.revision,
+        } })
         await invoke({ namespace: 'credentials', method: 'set', args: { ref, value: apiKey } })
         return { ok: true }
       } catch {
diff --git a/apps/desktop/src/welcome-window.ts b/apps/desktop/src/welcome-window.ts
index a32e1f1..a2feacf 100644
--- a/apps/desktop/src/welcome-window.ts
+++ b/apps/desktop/src/welcome-window.ts
@@ -77,10 +77,11 @@ export async function openWelcomeWindow(locale: DesktopLocale, operations: Welco
     }
   }
   ipcMain.handle(WELCOME_IPC.takeNotice, async (event) => { assertSender(event); return operations.takeNotice() })
-  ipcMain.handle(WELCOME_IPC.saveApiKey, async (event, value: unknown) => {
+  ipcMain.handle(WELCOME_IPC.saveApiKey, async (event, value: unknown, region: unknown = 'cn') => {
     assertSender(event)
     if (typeof value !== 'string' || !/^[\x21-\x7e]+$/.test(value)) return { ok: false }
-    return operations.saveApiKey(value)
+    if (typeof region !== 'string' || !['cn', 'sgp', 'ams'].includes(region)) return { ok: false }
+    return operations.saveApiKey(value, region)
   })
   ipcMain.handle(WELCOME_IPC.skip, async (event) => {
     assertSender(event)
diff --git a/packages/experimental/voice-input-bundle/cordis.patch.yml b/packages/experimental/voice-input-bundle/cordis.patch.yml
index 255734d..82cb4a2 100644
--- a/packages/experimental/voice-input-bundle/cordis.patch.yml
+++ b/packages/experimental/voice-input-bundle/cordis.patch.yml
@@ -1,13 +1,14 @@
+# Mio: voice input transcribes with MiMo ASR through the account the MiMo route already uses.
 - insert:
     - id: speech-to-text
       name: '@deepseek-ai/dsh-experimental-speech-to-text'
       config:
-        defaultProvider: sensevoice-local
-    - id: speech-to-text-sensevoice
-      name: '@deepseek-ai/dsh-experimental-speech-to-text-sensevoice'
-      config:
-        dataRoot: !!js dshHomePath('speech-to-text', 'sensevoice')
+        defaultProvider: mio-asr
+    - id: mio-asr
+      name: '@mio/asr'
     - id: api-speech-to-text
       name: '@deepseek-ai/dsh-experimental-api-speech-to-text'
+      config:
+        maxDurationSeconds: 60
     - id: ui-voice-input
       name: '@deepseek-ai/dsh-experimental-client-ui-voice-input'
diff --git a/packages/experimental/voice-input-bundle/locale/en.json b/packages/experimental/voice-input-bundle/locale/en.json
index 8d48bbe..197918e 100644
--- a/packages/experimental/voice-input-bundle/locale/en.json
+++ b/packages/experimental/voice-input-bundle/locale/en.json
@@ -1,6 +1,6 @@
 {
   "meta": {
     "title": "Voice input",
-    "description": "Transcribe recordings locally with SenseVoice; first use requires installing dependencies"
+    "description": "Transcribe recordings with MiMo ASR using your MiMo account; audio is sent to the MiMo API"
   }
 }
diff --git a/packages/experimental/voice-input-bundle/locale/zh.json b/packages/experimental/voice-input-bundle/locale/zh.json
index 43b2e83..9aa8c9a 100644
--- a/packages/experimental/voice-input-bundle/locale/zh.json
+++ b/packages/experimental/voice-input-bundle/locale/zh.json
@@ -1,6 +1,6 @@
 {
   "meta": {
     "title": "语音输入",
-    "description": "在本机使用 SenseVoice 转写录音；首次使用需安装依赖"
+    "description": "使用 MiMo ASR 和你的 MiMo 账号转写录音；音频会发送至 MiMo API"
   }
 }
diff --git a/packages/experimental/voice-input-bundle/package.json b/packages/experimental/voice-input-bundle/package.json
index 7c63262..3a39f46 100644
--- a/packages/experimental/voice-input-bundle/package.json
+++ b/packages/experimental/voice-input-bundle/package.json
@@ -1,6 +1,6 @@
 {
   "name": "@deepseek-ai/dsh-experimental-voice-input-bundle",
-  "description": "Experimental voice input with local SenseVoice; downloads its runtime on first use",
+  "description": "Voice input transcribed by MiMo ASR",
   "version": "0.1.7-rc.2",
   "icon": "./icon.svg",
   "publishConfig": {
@@ -34,9 +34,9 @@
   "license": "MIT",
   "dependencies": {
     "@deepseek-ai/dsh-experimental-speech-to-text": "workspace:*",
-    "@deepseek-ai/dsh-experimental-speech-to-text-sensevoice": "workspace:*",
     "@deepseek-ai/dsh-experimental-api-speech-to-text": "workspace:*",
-    "@deepseek-ai/dsh-experimental-client-ui-voice-input": "workspace:*"
+    "@deepseek-ai/dsh-experimental-client-ui-voice-input": "workspace:*",
+    "@mio/asr": "workspace:*"
   },
   "peerDependencies": {
     "@deepseek-ai/cordis": "workspace:~"
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 36dc050..4f2af24 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -726,6 +726,9 @@ importers:
       '@deepseek-ai/libreoffice-kit':
         specifier: ^0.1.1
         version: 0.1.1
+      '@mio/desktop':
+        specifier: workspace:*
+        version: link:../../mio/desktop
 
   apps/web:
     devDependencies:
@@ -931,6 +934,48 @@ importers:
         specifier: ^1.49.0
         version: 1.61.1
 
+  mio/asr:
+    dependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:~
+        version: link:../../vendor/cordis
+      '@deepseek-ai/dsh-credentials':
+        specifier: workspace:*
+        version: link:../../packages/credentials/credentials
+      '@deepseek-ai/dsh-experimental-speech-to-text':
+        specifier: workspace:*
+        version: link:../../packages/experimental/speech-to-text
+      '@deepseek-ai/schemastery':
+        specifier: link:../../vendor/schemastery
+        version: link:../../vendor/schemastery
+
+  mio/brand:
+    dependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:~
+        version: link:../../vendor/cordis
+
+  mio/desktop:
+    dependencies:
+      '@mio/brand':
+        specifier: workspace:*
+        version: link:../brand
+      '@mio/tts':
+        specifier: workspace:*
+        version: link:../tts
+
+  mio/tts:
+    dependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:~
+        version: link:../../vendor/cordis
+      '@deepseek-ai/dsh-credentials':
+        specifier: workspace:*
+        version: link:../../packages/credentials/credentials
+      '@deepseek-ai/schemastery':
+        specifier: link:../../vendor/schemastery
+        version: link:../../vendor/schemastery
+
   native/system:
     devDependencies:
       '@deepseek-ai/node-addon-system':
@@ -7897,9 +7942,9 @@ importers:
       '@deepseek-ai/dsh-experimental-speech-to-text':
         specifier: workspace:*
         version: link:../speech-to-text
-      '@deepseek-ai/dsh-experimental-speech-to-text-sensevoice':
+      '@mio/asr':
         specifier: workspace:*
-        version: link:../speech-to-text-sensevoice
+        version: link:../../../mio/asr
     devDependencies:
       '@deepseek-ai/cordis':
         specifier: workspace:~
diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
index 4782ad5..dcbf496 100644
--- a/pnpm-workspace.yaml
+++ b/pnpm-workspace.yaml
@@ -1,4 +1,5 @@
 packages:
+  - mio/*
   - vendor/*
   - packages/*/*
   # The Landlock launcher is developed with its harness consumers but keeps
diff --git a/scripts/client-build-environment.ts b/scripts/client-build-environment.ts
index 57f12ef..d81d1fa 100644
--- a/scripts/client-build-environment.ts
+++ b/scripts/client-build-environment.ts
@@ -19,7 +19,7 @@ export const CLIENT_BUILD_PROFILE_SELECTOR = 'DSH_BUILD_CLIENT_PROFILE'
 /** Public client environment required by official DSH artifacts. */
 const OFFICIAL_CLIENT_BUILD_ENVIRONMENT = {
   DSH_CLIENT_BUILD_PROFILE: 'official',
-  DSH_CLIENT_TITLE: 'DeepSeek Harness',
+  DSH_CLIENT_TITLE: 'Mio',
 } as const
 
 /** Public variable carrying the source commit embedded in client artifacts. */
